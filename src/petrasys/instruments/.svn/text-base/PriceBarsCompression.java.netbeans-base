/*
 * //rpc - WORKING HERE:3/30/10 11:13 AM - We need to expand and contract PriceBars for different
 * time intervals. Boundaries for a given interval need to be specified as well, where to begin
 * the set of bars,
 */

package petrasys.instruments;

import petrasys.utils.TestFrame;

/**
 *
 * @author rickcharon
 */
public class PriceBarsCompression {
  private PriceBars priceBarsOrig;
  public final int HOUR = 60;
  public final int DAY = 60 * 24;
  public final int WEEK = 60 * 24 * 7;


  public PriceBarsCompression(PriceBars pbsIn) {

  }

//  PriceBars compress(int minutes) {
//    //PriceBars pbOut = new PriceBars();
//
//    return pbOut;
//  }

  public static void main(String args[]) {
    TestFrame jf = new TestFrame();
    jf.setVisible(true);
  }

}
